# Dependencias
# Ignora la carpeta que contiene todos los paquetes que instalamos con npm.
node_modules/

# Archivos de producción y builds
/dist
/build

# Archivos de Logs
# Ignora cualquier archivo que termine en .log y los logs de debug de npm.
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*

# Archivos de entorno
# ¡EL MÁS IMPORTANTE! Ignora el archivo que contiene tus contraseñas y claves secretas.
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Archivos del sistema operativo
# Ignora archivos basura generados por macOS, Windows y Linux.
.DS_Store
Thumbs.db
.history

# Reportes de tests
coverage/
